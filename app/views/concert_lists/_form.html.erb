<%= form_for(@concert_list) do |f| %>
  <% if @concert_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concert_list.errors.count, "error") %> prohibited this concert_list from being saved:</h2>

      <ul>
      <% @concert_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :list_owner_id %><br>
    <%= f.number_field :list_owner_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
