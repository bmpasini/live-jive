<%= form_for(current_user.concert_goings.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :concert_id, @concert.id %></div>
  <div style="text-align: center;">
    <%= f.submit "RSVP", class: "btn btn-primary", style: "width: 400px;" %>
  </div>
<% end %>