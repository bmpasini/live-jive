<% provide(:title, @title) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <%= gravatar_for @band %>
      <h1><%= @band.name %></h1>
      <span style="font-size: 14px; margin-top: 10px;"><%= link_to "view profile", @band %></span>
    </section>
    <section class="stats">
      <%= render 'bands/stats' %>
      <% if @bands.any? %>
        <div class="user_avatars">
          <% @bands.each do |band| %>
            <%= link_to gravatar_for(band, size: 30), band %>
          <% end %>
        </div>
      <% end %>
    </section>
  </aside>
  <div class="col-md-8">
    <h3><%= @title %></h3>
    <% if @bands.any? %>
      <ul class="users follow">
        <%= render @bands %>
      </ul>
      <%= will_paginate %>
    <% end %>
  </div>
</div>