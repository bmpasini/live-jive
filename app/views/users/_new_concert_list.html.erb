<% if current_user?(@user) %>
	<div style="text-align: center;">
		<%= button_to "Create a new concert list", new_concert_list_url, 
      :class => "btn btn-primary", :method => :get %>
	</div>
<% end %>